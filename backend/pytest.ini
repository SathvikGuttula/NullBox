[pytest]
pythonpath = .
testpaths = tests

# Tests that download a pretrained encoder or need a GPU are opt-in:
#   pytest -m slow        run only those
#   pytest                run everything else (the default)
addopts = -m "not slow" --strict-markers

markers =
    slow: needs the pretrained encoder download or a GPU
