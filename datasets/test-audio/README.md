# Local test corpus

Generated by `python backend/scripts/make_test_audio.py`. Not committed
(`*.wav` is gitignored) - regenerate it on any machine in a few seconds.

## spoof/

Windows SAPI text-to-speech. A different synthesis family from anything
in ASVspoof 2019 LA, so this is genuinely out-of-distribution for the
checkpoint - and it is still detected, at 0.956-0.977 raw / 1.000
calibrated. Every file here **should** be flagged.

| file | voice |
| --- | --- |
| `tts_MicrosoftDavidDesktop_transfer.wav` | Microsoft David Desktop |
| `tts_MicrosoftHazelDesktop_transfer.wav` | Microsoft Hazel Desktop |
| `tts_MicrosoftZiraDesktop_transfer.wav` | Microsoft Zira Desktop |
| `tts_MicrosoftDavidDesktop_short.wav` | Microsoft David Desktop |
| `tts_MicrosoftHazelDesktop_short.wav` | Microsoft Hazel Desktop |
| `tts_MicrosoftZiraDesktop_short.wav` | Microsoft Zira Desktop |
| `tts_MicrosoftDavidDesktop_long.wav` | Microsoft David Desktop |
| `tts_MicrosoftHazelDesktop_long.wav` | Microsoft Hazel Desktop |
| `tts_MicrosoftZiraDesktop_long.wav` | Microsoft Zira Desktop |

## edge/

The inputs a tester tries first. Most of these **should be refused**,
not scored: the detector is a speech model, and asked about silence or
noise it answers confidently and wrongly (0.999 and 0.997 synthetic
respectively, measured). A refusal reports `model_status: no_speech`
and drops the branch from the fusion.

| file | expected |
| --- | --- |
| `silence.wav` | REFUSED - no_speech. The model has never seen silence; uncaught it scores 0.999 synthetic. |
| `white_noise.wav` | REFUSED - no_speech. The energy VAD passes this; the speech gate is what stops it. |
| `pure_tone.wav` | REFUSED - no_speech. Steady signal, no gaps between words. |
| `dial_tone.wav` | REFUSED - no_speech. A real dial tone is 350 Hz + 440 Hz. |
| `mains_hum.wav` | REFUSED - no_speech. 50 Hz hum with harmonics. |
| `dc_offset.wav` | REFUSED - no_speech. Constant signal. |
| `too_short.wav` | HTTP 400 - under the one-second minimum. |
| `speech_stereo.wav` | DETECTED. Channels are averaged to mono on the way in. |
| `speech_8k.wav` | DETECTED. Resampled to 16 kHz on the way in - this is the telephone-bandwidth case. |
| `speech_48k.wav` | DETECTED. Resampled down to 16 kHz. |
| `speech_clipped.wav` | DETECTED. Heavy clipping does not rescue a synthetic sample. |
| `speech_quiet.wav` | DETECTED. Quiet but well above the silence floor; the detector peak-normalises before scoring. |
| `speech_muted.wav` | REFUSED - no_speech. Below -60 dBFS is a muted microphone, and amplifying it would score the noise floor. |
| `not_audio.wav` | HTTP 400 - cannot be decoded, with a message saying so. |
| `empty.wav` | HTTP 400 - the file is empty. |

## bonafide/

Deliberately empty. Genuine human speech has to come from a microphone,
and nothing in this script can fabricate it - a synthetic stand-in would
be a spoof sample mislabelled as bonafide, which is worse than an empty
folder. Record your own through the UI (see TESTING.md).

Without bonafide samples you can measure detection but **not** false
alarms. The published 0.79% false-alarm rate comes from 3,678 held-out
bonafide utterances in the evaluation set, not from anything here.
